apiVersion: 1

contactPoints:
  - orgId: 1
    name: ticket-email
    receivers:
      - uid: email-default
        type: email
        settings:
          addresses: ryanjewik25@gmail.com           # <-- CHANGE ME
          singleEmail: true
          message: |
            {{ range .Alerts }}
            *{{ .Labels.alertname }}* ({{ .Labels.source }})
            Value: {{ .ValueString }}
            URL: {{ .Labels.url }}
            {{ .Annotations.description }}
            {{ end }}
