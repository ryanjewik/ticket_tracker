apiVersion: 1

policies:
  - orgId: 1
    receiver: ticket-email
    group_by: ['alertname','source']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    # Optionally route by labels here if you add more teams later
